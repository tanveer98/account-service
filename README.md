## Project structure
* [frontend](./frontend) -- angular SPA to run the frontend, requires node v26.3.1, use `ng serve` to run
* [backend](./backend) -- spring boot webapp, requires JDK 21, use `./gradlew bootRun` to run

## Demo
![Demo video](./demo/demo-video.mp4)